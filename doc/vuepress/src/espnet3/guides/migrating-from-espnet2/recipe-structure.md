# Recipe structure

Main changes:

- `run.sh` becomes `run.py`
- numbered stages become named stages
- dataset code moves into `dataset/`
- Python helpers live in `src/`
- config files are split into `training.yaml`, `inference.yaml`,
  `metrics.yaml`, `publication.yaml`, and `demo.yaml`

