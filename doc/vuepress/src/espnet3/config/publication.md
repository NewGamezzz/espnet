# Publication config

`publication.yaml` controls `pack_model` and `upload_model`.

Common top-level areas are:

- `exp_dir`
- `inference_dir`
- `pack_model`
- `upload_model`

Use `inference_dir`, not `decode_dir`.

