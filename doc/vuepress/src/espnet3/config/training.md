# Training config

`training.yaml` controls:

- `create_dataset`
- `train_tokenizer`
- `collect_stats`
- `train`

Common top-level areas are dataset definition, tokenizer settings, model,
trainer, optimizer, scheduler, and experiment paths.

