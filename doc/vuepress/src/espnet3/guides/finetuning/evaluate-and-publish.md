# Evaluate and publish

The usual fine-tune close-out flow is:

1. run `infer`
2. run `measure`
3. inspect `metrics.json`
4. run `pack_model`
5. run `upload_model`

Use [Publish stage](../../stages/publish.md) for the exact bundle behavior.

