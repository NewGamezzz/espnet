"""SSSD-to-BagPiper LM-TTS data pipeline (see PLAN-step2).

Currently exposes only the ported preprocessing primitives
(``dataset.preprocessing.sssd``, ``dataset.preprocessing.windows``); the
BagPiper-specific builder/dataset/preprocessor orchestration lands in later
tasks.
"""
